// ContentView has moved to App-Sputnik/ContentView.swift (ISS-NEW-C).
// Foundation must not import TextEditorModule or TerminalModule — assembly lives
// at the app layer, not inside the Foundation package.
