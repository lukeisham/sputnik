# Issues

Logged by `!TrackIssues`. Append only — do not edit or delete existing rows.

| ID | Date | Module | Type | Description | Severity | Rule Violated | File | Status |
|---|---|---|---|---|---|---|---|---|
| ISS-001 | 2026-06-08 | 2 Foundation (2.4 / 2.5) | Bug | The persisted layout struct is named `PanelLayout` in the 2.4 UI/UX guide but `LayoutState` in the 2.5 Persistence guide — the same data has two conflicting names, which would create two divergent types if implemented literally. | Medium | SR-1 (shared types defined once in Foundation) | 1 Setup/Module Guides/2 Foundation/2.4 UI and UX/guide.md; .../2.5 Persistence/guide.md | Resolved — 2026-06-08: `LayoutState` (2.5) is the persisted root; `PanelLayout` (2.4) is its panel-arrangement component. Clear containment relationship: `LayoutState` ⊃ `PanelLayout`. |
| ISS-002 | 2026-06-08 | 7 Terminal / 2.3 Settings | Missing Feature | The 7 Terminal guide states `TerminalProfile` (font, colours, scrollback line limit) is sourced from Settings (2.3), but the 2.3 Settings guide's owned-state list defines no terminal fields, so the profile-from-settings data flow has no backing store. | Medium | SR-1 (shared settings owned once in Foundation 2.3) | 1 Setup/Module Guides/7 Terminal/guide.md; .../2.3 Settings/guide.md | Resolved — 2026-06-09: `SettingsStore` fields and consumer wiring confirmed complete; `TerminalView` reads from `@Environment(SettingsStore.self)`; no local defaults remain. |
| ISS-003 | 2026-06-08 | 2 Foundation (2.4) / 7 Terminal | Bug | A shared thrown-error type `SputnikError` (e.g. `.hardwareAccessDenied`) is referenced by the 7 Terminal guide, the Vibe SW-4 example, and the 2.7 guide, but no Foundation guide defines it — 2.4 defines only `SputnikAlert` for dialog presentation, leaving thrown errors with no shared type. | Medium | SR-1 (shared types defined once in Foundation) | 1 Setup/Module Guides/7 Terminal/guide.md; .../2.4 UI and UX/guide.md; 1 Setup/Vibe_Coding_Rules.md | Resolved — 2026-06-08: `SputnikError` authored in `2 Foundation/2.4 UI and UX/SputnikError.swift` with `.hardwareAccessDenied(detail:)`, `.processLaunchFailed(detail:)`, and `.ptyWriteFailed` cases. Foundation owns the type; Terminal references it only (SR-1). |
| ISS-004 | 2026-06-08 | 3 Text Editor / 2.3 Settings | Missing Feature | Editor sub-modules 3.1–3.5 expect to source several settings from 2.3 (auto-save interval, ghost-text debounce interval, suggestions-enabled toggle, ASCII trigger key binding, spell-check locale), but the 2.3 Settings guide's owned-state list defines none of them — it has only a boolean `autoSaveEnabled` and `spellCheckEnabled`/`grammarCheckEnabled`. | Medium | SR-1 (shared settings owned once in Foundation 2.3) | 1 Setup/Module Guides/3 Text Editor Window/3.1 Text/guide.md; .../2.3 Settings/guide.md | Resolved — 2026-06-09: `SettingsStore` fields and consumer wiring confirmed complete; all six editor files inject and use `SettingsStore`; no local defaults remain. |
| ISS-005 | 2026-06-08 | 2 Foundation (2.2) / 2.4 | Missing Feature | `AppState` tracks only a single `currentlyOpenFile`/`currentlyOpenFileType`, so it cannot represent multiple files open at once as tabs; the spec's "Tabs and Windows" (2.4) and "Editor Window … opens either text or binary files or Markdown files" have no backing multi-document state model, and there is nothing to bind each preview panel to the file it is rendering. | Medium | SR-1 (shared multi-document state owned once in Foundation 2.2) | 1 Setup/Module Guides/2 Foundation/2.2 Global State Management/guide.md; .../2.4 UI and UX/guide.md | Resolved — 2026-06-08: `DocumentSession` added (2.2); `AppState` now owns `openDocuments: [DocumentSession]` and `activeDocumentID: UUID?`; `currentlyOpenFile`/`currentlyOpenFileType` become read-only computed wrappers; `DocumentTabBar` (2.4) drives active-tab selection; `LayoutState` (2.5) persists open-tab URLs across launches; `InterPanelRouter` protocol updated with find-or-create `open(_:)` and new `close(_:)`. |
| ISS-006 | 2026-06-08 | 8 HTML Preview / 4 Markdown Preview | Missing Feature | No Module Guide exists for the preview panels (folders `8 HTML Preview/` and `4 Markdown Preview/` contain no guide.md), so link-click navigation is undefined — a raw `WKWebView` navigates in place when a link is clicked, replacing the rendered file and silently desyncing the preview from the editor instead of opening the target as a new tab. | Medium | — | 1 Setup/Module Guides/8 HTML Preview/ (absent); 1 Setup/Module Guides/4 Markdown Preview/ (absent) | Resolved — 2026-06-08: Both modules have guides + source. Module 8: `HTMLPreviewView`, `HTMLPreviewCoordinator`, `LinkNavigationPolicy`. Module 4: `MarkdownPreviewPanel`, `MarkdownRenderView`, `MarkdownPreviewCoordinator`, `MarkdownPreviewViewModel`. Both panels are pure functions of `AppState.activeDocument`; link interception handles local/external/anchor navigations. |
| ISS-007 | 2026-06-08 | 5 PDF Viewer / 4 Markdown Preview / 2.4 PanelLayout | Bug | `PanelLayout.default` assigns `.centerLower: .pdfViewer` and `.right: .markdownPreview`, but the Module 4 guide says Markdown Preview occupies "centre lower" and the Module 5 guide says PDF Viewer is "in Sputnik's right slot." The guides and the default layout swap the two panels' slots. | Low | — (guides vs code inconsistency) | 2 Foundation/2.4 UI and UX/PanelLayout.swift; 1 Setup/Module Guides/4 Markdown Preview/guide.md; 1 Setup/Module Guides/5 PDF Viewer/guide.md | Resolved — 2026-06-08: `PanelLayout.default` updated: `.centerLower: .markdownPreview`, `.right: .pdfViewer`, matching both module guides.
| ISS-008 | 2026-06-08 | 2 Foundation (2.2 / 2.4) / 9 Resources | Missing Feature | Foundation's `AppState.requestedHelpTopic` carries only a `HelpTopic` kind (markdown/html/asciiArt/grammar), not a target topic ID, so each help coordinator reinvents "navigate to a specific topic" differently — Grammar posts a `NotificationCenter` notification, HTML uses an `openTopicHandler` closure, Markdown and ASCII use `async` methods returning `Bool` — leaving no single Foundation route for another module (e.g. the editor) to open a help panel to a specific topic. | Medium | SR-1 (cross-module comms route through Foundation; shared primitives defined once) | 2 Foundation/2.2 Global State Management/AppState.swift:83; 2 Foundation/2.4 UI and UX/HelpTopic.swift; 9 Resources/9.5 Grammar Help/GrammarHelpCoordinator.swift:128; 9 Resources/9.4 Html Help/HTMLHelpCoordinator.swift:95 | Resolved — 2026-06-09: `HelpRequest` + `AppState.requestedHelpTarget` is the single Foundation route; `GrammarHelpCoordinator` NotificationCenter path and `openTopicHandler` closures in Markdown/HTML coordinators removed; all three coordinators use `onNavigate` wired to `AppState` weakly from their panel views. |
| ISS-009 | 2026-06-08 | 9 Resources (9.5 Grammar Help) | Bug | `index.json` (the runtime source of truth decoded by `GrammarHelpIndex.ensureLoaded()`, with each topic body embedded inline) listed only 26 of the 55 topic `.md` files on disk. 29 topics — all of `edge-cases/`, `mechanics/`, `sentence-structure/`, plus 4 punctuation, 2 spelling, and 6 usage files — had no index entry, so they never loaded, searched, or appeared in the Grammar Help panel or the right-click context lookup. | Medium | SR-1 (each topic indexed once); Sputnik rule 1 (module self-contained/consistent) | 9 Resources/9.5 Grammar Help/index.json; .../GrammarHelpIndex.swift | Resolved — 2026-06-08: `index.json` regenerated from all 55 `.md` files; bodies taken verbatim (H1→H2), curated `searchTerms`/`relatedTopics` preserved for the existing 26, hand-authored for the 29 new topics. All `relatedTopics` verified to resolve; no duplicate ids. |
| ISS-010 | 2026-06-09 | 8 HTML Preview / 9 Resources | Design Blocker | `HTMLPreviewView` disables all JavaScript via the deprecated `configuration.preferences.javaScriptEnabled = false`. Reading the user's text selection from a `WKWebView` (needed for the new "More Context" right-click gesture) requires JS (`window.getSelection().toString()`), so the HTML-preview More Context lookup cannot capture the selected word as configured. | Medium | SR-5 (use native frameworks correctly); SW-3 (AppKit/WebKit bridge) | 8 HTML Preview/HTMLPreviewView.swift:70 | Resolved — 2026-06-09: implemented in `8 HTML Preview/HTMLPreviewView.swift` and `HTMLPreviewCoordinator.swift` as part of plan "2026-06-09 2 Foundation More Context shared lookup utility" (now in Plans Completed). Deprecated `javaScriptEnabled` removed; `configuration.defaultWebpagePreferences.allowsContentJavaScript = false` + `WKUserScript` selection-change listener + `WKScriptMessageHandler` on the coordinator are all in place. |
| ISS-011 | 2026-06-09 | 2 Foundation (2.3 Settings) / 3 Text Editor | Design Problem | The planned per-language × per-function writing-assist toggle matrix (Spelling/Grammar/Markdown/HTML/ASCII × Instant Correct/Auto-Complete/More Context) overlaps the two existing flat booleans `SettingsStore.spellCheckEnabled` and `grammarCheckEnabled`. If the matrix is added alongside them, "is spell/grammar checking on?" would have two sources of truth, and the Edit ▸ Spelling and Grammar menu toggles would diverge from the new matrix. | Medium | SR-1 (each setting owned once in Foundation 2.3) | 2 Foundation/2.3 Settings/SettingsStore.swift; 2 Foundation/2.0 App Overview/SputnikCommands.swift | Resolved — 2026-06-09: `WritingAssistMatrix` is the single source; `spellCheckEnabled`/`grammarCheckEnabled` are computed over `spelling/grammar × instantCorrect`; legacy keys migrated on first load; `Writing Assistance` submenu in Edit menu. |
| ISS-012 | 2026-06-09 | 2.3 Settings / 3 Text Editor | Design Problem | `SettingsStore.editorFont` is a single global token consumed by the Text Editor, File Tree, and Terminal. The new per-panel font feature (F-4) requires separate font values for Text Editor, Markdown Preview, and HTML Preview. Adding new fields without removing the old one would leave two sources of truth for the Text Editor's font. | Medium | SR-1 (each setting owned once in Foundation 2.3) | 1 Setup/Module Guides/2 Foundation/2.3 Settings/guide.md | Resolved — 2026-06-09: F-4 resolved by adding nullable per-panel override fields (`textEditorFont?`, `markdownPreviewFont?`, `htmlPreviewFont?`) with computed `resolved*` helpers that fall back to global `editorFont`; File Tree and Terminal continue to consume `editorFont` unchanged. |
| ISS-013 | 2026-06-09 | 2.2 Global State / 2.4 UI/UX | Missing Feature | `AppState` has no `isProcessing` flag or counter. The bottom status bar (F-5) needs a way to animate the Sputnik icon during any background operation, and the menu-bar status item (F-1) needs the same signal. Without a shared flag, each module would need its own indicator, diverging from SR-1. | Medium | SR-1 (shared state owned once in Foundation 2.2) | 1 Setup/Module Guides/2 Foundation/2.2 Global State Management/guide.md | Resolved — 2026-06-09: F-5 resolved by adding `beginProcessing()` / `endProcessing()` counter methods to `AppState`; `isProcessing` is a computed `Bool` (counter > 0); all long operations bracket their work with these calls. |
| ISS-014 | 2026-06-09 | 2.3 Settings / Security | Design Problem | `PersistenceService` persists all `SettingsStore` values to `UserDefaults`. Storing AI API keys in `UserDefaults` is insecure — the plist is readable by any process with the same sandbox entitlements and is included in unencrypted backups by default. The planned AI credentials feature (F-3) requires a Keychain bridge before it can be implemented safely. | High | SR-2 (handle all failure paths; implicit: do not store credentials insecurely) | 1 Setup/Module Guides/2 Foundation/2.3 Settings/guide.md; 1 Setup/Module Guides/2 Foundation/2.5 Persistence/guide.md | Resolved — 2026-06-09: F-3 resolved by introducing `KeychainService` in 2.7 Utilities; the API key is never written to `UserDefaults` or held in `SettingsStore`; only `AIConfiguration` (model name + base URL) is persisted via `PersistenceService`. |
| ISS-015 | 2026-06-09 | 2.3 Settings | Design Problem | `AIConfiguration` and `SettingsStore.aiConfig` are named generically. With two distinct AI roles now defined (Supporting AI for resource features, Main AI user-loaded in terminal), the generic name creates ambiguity — any code calling `SettingsStore.aiConfig` does not self-document which role it serves. Violates SR-1 (types defined once, clearly named). | Medium | SR-1 (shared types named once, unambiguously) | 2 Foundation/2.3 Settings/AIConfiguration.swift; 2 Foundation/2.3 Settings/SettingsStore.swift | Resolved — 2026-06-09: `AIConfiguration` is a deprecated typealias for `SupportingAIConfiguration`; `SettingsStore.aiConfig` renamed to `supportingAIConfig`.
| ISS-016 | 2026-06-09 | 2.2 Global State / 2.3 Settings | Design Problem | `AppState.contextUsage: ContextUsage?` is documented as "written by any module making AI calls." With two AI roles — Supporting AI (resource features, cumulative per-launch token count shown in Settings) and Main AI (terminal-hosted, per-session context window shown in status bar) — one field cannot represent both roles without conflating them. Violates SR-1 (each value owned once with one clear meaning). | Medium | SR-1 | 2 Foundation/2.2 Global State Management/AppState.swift; 2 Foundation/2.2 Global State Management/ContextUsage.swift | Resolved — 2026-06-09: `AppState.contextUsage` removed; replaced by `supportingAIUsage: SupportingAIUsage?` and `mainAIState: MainAIState?`; `ContextUsage` is a deprecated typealias for `MainAIContextUsage`.
| ISS-017 | 2026-06-10 | 2.6 App Lifecycle | Bug | A stale 505-line duplicate of the app entry point lives at `2 Foundation/2.6 App Lifecycle/SputnikApp.swift`, duplicating the real `App-Sputnik/SputnikApp.swift`; it is kept out of the build only by a single hardcoded `exclude` in `2 Foundation/Package.swift`, and its own header comment falsely claims the file is "intentionally empty." Removing or copying that exclude line would reintroduce a second `@main`. | Low | SR-6 (one responsibility per file; no stale duplicates) | 2 Foundation/2.6 App Lifecycle/SputnikApp.swift; 2 Foundation/Package.swift:15 | Resolved — 2026-06-10: Deleted `2 Foundation/2.6 App Lifecycle/SputnikApp.swift`; removed the hardcoded `exclude` from `2 Foundation/Package.swift`. Build confirmed @main-free; error count dropped 314→164. |
| ISS-018 | 2026-06-10 | 2.0 App Overview | Bug | "Merge All Windows" moves tabs and de-registers the merged windows from `AppState`, but the loop meant to close their leftover `NSWindow`s is an empty body (with a comment admitting it cannot map `NSWindow` to `WindowState`), so emptied native windows can linger on screen until SwiftUI happens to reconcile them. | Medium | SR-2 (handle every path explicitly) | 2 Foundation/2.0 App Overview/SputnikCommands.swift:429 | Resolved — 2026-06-10: Each NSWindow is tagged on appear with `NSWindow.identifier = NSUserInterfaceItemIdentifier(windowState.id.uuidString)` in `SputnikApp.onAppear`; "Merge All Windows" now closes each merged window via `NSApp.windows.first(where: { $0.identifier?.rawValue == window.id.uuidString })?.close()`. |
| ISS-019 | 2026-06-10 | 2.4 UI and UX | Missing Feature | `DocumentTabBar` has no drag-to-reorder gesture; tabs are rendered only in append order and the user cannot rearrange them, and any reordered order would not persist via `WindowDescriptor.openTabURLs`. | Low | — | 2 Foundation/2.4 UI and UX/DocumentTabBar.swift | Resolved — 2026-06-10: Added `onDrag`/`onDrop` to `DocumentTabBar` using `NSItemProvider` (UUID string payload); drag-source dims to 0.4 opacity. `WindowState.moveDocument(fromOffsets:toOffset:)` reorders `openDocuments` in place; `AppState` delegates to `activeWindow`. Reordered order persists via `WindowDescriptor.openTabURLs` which serialises the live array order. |
| ISS-020 | 2026-06-10 | 2.0 App Overview / 2.1 Inter-Panel | Bug | "Move Tab to New Window" removes the active `DocumentSession` directly instead of routing through the router's `close(_:)` path, so it bypasses the unsaved-changes (`isDirty`) guard that the tab-bar close button honours, risking silent loss of unsaved edits when a dirty tab is moved. | Medium | SR-2 (handle every failure path; no data loss) | 2 Foundation/2.0 App Overview/SputnikCommands.swift:394 | Resolved — 2026-06-10: Added `moveActiveTabToNewWindow() async -> UUID?` to `AppInterPanelRouter`; shows an `NSAlert` confirmation when `session.isDirty`, returns `nil` on cancel. `SputnikCommands` now receives the router and calls `Task { await router.moveActiveTabToNewWindow() }`; `openWindow` fires only on confirmed non-nil return. |
| ISS-021 | 2026-06-10 | 7 Terminal | Bug | `TerminalTextView` returns `acceptsFirstResponder = true` and implements `keyDown(with:)`, but nothing ever makes the view first responder — `TerminalRenderer.makeNSView` never calls `makeFirstResponder`, there is no `viewDidMoveToWindow`, and no `mouseDown` override to focus on click — so `keyDown` never fires and the user cannot type any command into the shell. | High | SR-2 (handle every path; core function must work) | 7 Terminal/TerminalTextView.swift:64; 7 Terminal/TerminalRenderer.swift:34 | Resolved — 2026-06-10: `viewDidMoveToWindow()` → `makeFirstResponder(self)`, `mouseDown(with:)` → `makeFirstResponder(self)`, `acceptsFirstMouse` → `true`; TerminalRenderer wires `onKeyInput` in `updateNSView` as well as `makeNSView`. |
| ISS-022 | 2026-06-10 | 7 Terminal | Bug | `TerminalRenderer.Coordinator` stores an `onResize` closure and declares an `observer` property but never registers for `NSView.frameDidChangeNotification` (and `TerminalTextView` never sets `postsFrameChangedNotifications`), so `onResize` is never called and the emulator stays hardcoded at the `cols: 80, rows: 24` passed in `TerminalManager.startSession`; output wraps at the wrong width and full-screen TUI/agent CLIs render corrupted. | Medium | SR-2 (handle every path) | 7 Terminal/TerminalRenderer.swift:51; 7 Terminal/TerminalManager.swift:62 | Resolved — 2026-06-10: `TerminalTextView` sets `postsFrameChangedNotifications = true`, observes `frameDidChangeNotification` in `viewDidMoveToWindow`, emits de-duped cols/rows via `onResize`; `TerminalRenderer` wires `view.onResize` in `makeNSView`/`updateNSView`; `TerminalManager.resize` resizes both PTY + emulator; session-start seeds real size. `Coordinator.observer` removed. |
| ISS-023 | 2026-06-10 | 2.4 UI and UX | Bug | `SputnikAlert.message` builds quoted-filename strings as `return ""\(url.lastPathComponent)" ..."` — the leading inner double-quote is unescaped, so the literal closes immediately and the interpolation is parsed as loose code, producing 12 "consecutive statements"/"string interpolation outside a string literal" compile errors. Inner quotes must be escaped as `\"`. | High | SR-2 (code must compile/handle every path) | 2 Foundation/2.4 UI and UX/SputnikAlert.swift:39 | Resolved — 2026-06-11: All message strings use `\"` escaping (lines 40, 42, 44); confirmed in commit 7b13b5b. |
| ISS-024 | 2026-06-10 | 2.7 Utilities | Bug | `ClaudeStatusLineReader.swift` and `TerminalModelDetector.swift` reference `AppState.terminalModelInfo`, a member removed in the ISS-015/016 AI-role refactor (replaced by `mainAIState`/`supportingAIUsage`); `MainAIMonitor.swift`'s own header states it "migrated from ClaudeStatusLineReader", so both files appear orphaned and break the build (8 errors). | High | SR-6 (no stale duplicates); SR-2 (must compile) | 2 Foundation/2.7 Utilities/ClaudeStatusLineReader.swift; 2 Foundation/2.7 Utilities/TerminalModelDetector.swift | Resolved — 2026-06-11: Both files deleted in commit 7b13b5b; stats polling migrated into `MainAIMonitor.swift`. |
| ISS-025 | 2026-06-10 | 2.7 Utilities | Bug | `MainAIMonitor.deinit` (and the equivalent in `TerminalModelDetector`) calls main-actor-isolated members (`processingTask`, `stopStatsPolling()`, `stopFileWatcher()`) from a nonisolated `deinit`, producing actor-isolation compile errors under Swift strict concurrency. | High | SW-1 (strict actor isolation); SR-2 | 2 Foundation/2.7 Utilities/MainAIMonitor.swift:87 | Resolved — 2026-06-11: `@MainActor deinit` adopted at line 87; confirmed in commit 7b13b5b. |
| ISS-026 | 2026-06-10 | 2.3 Settings / 2.4 UI and UX / 2.6 App Lifecycle | Bug | Assorted API drift breaks the build: `SettingsStore.DefaultsKey` has no `terminalFontName`; `SputnikColor` has no `accentPrimary` (DocumentTabBar); `HierarchicalShapeStyle.orange` misused in SupportingAISettingsView; a closure passed where a `SortComparator` is required in SlashCommandRegistry; and `NSApp.replyToApplicationShouldTerminate` uses the Swift-3-obsoleted spelling (should be `reply(toApplicationShouldTerminate:)`). | High | SR-2 (must compile); SR-5 (use current framework APIs) | 2 Foundation/2.3 Settings/SettingsStore.swift; 2 Foundation/2.4 UI and UX/DocumentTabBar.swift; 2 Foundation/2.3 Settings/SupportingAISettingsView.swift; 2 Foundation/2.7 Utilities/SlashCommandRegistry.swift; 2 Foundation/2.6 App Lifecycle/AppDelegate.swift:89 | Resolved — 2026-06-11: All five drift points fixed in commit 7b13b5b: (1) `terminalFontName` exists at SettingsStore.swift:150; (2) `accentPrimary` not referenced; (3) `HierarchicalShapeStyle.orange` not referenced; (4) `SortComparator` not used (closure `sorted { lhs, rhs in … }`); (5) `reply(toApplicationShouldTerminate:)` at AppDelegate.swift:89. |
| ISS-027 | 2026-06-10 | 2.7 Utilities | Bug | `MainAIMonitor` is `@MainActor` but conforms to `TerminalAIOutputObserving`, whose `observe(line:)` is invoked from the `TerminalSession` actor; the conformance crosses into main-actor-isolated code and "can cause data races" — a warning today, a hard error under the Swift 6 language mode. `observe(line:)` only yields to a `Sendable` `AsyncStream` continuation, so it should be declared `nonisolated`. | Medium | SW-1 (strict actor isolation, no data races) | 2 Foundation/2.7 Utilities/MainAIMonitor.swift:211 | Resolved — 2026-06-11: `observe(line:)` declared `nonisolated` at line 207; confirmed in commit 7b13b5b. |
| ISS-028 | 2026-06-10 | 2.2 Global State / 2.4 UI and UX | Bug | `WindowState.title` (the workspace folder name, `activeWorkspaceDirectory?.lastPathComponent ?? "Untitled"`) is defined but never applied to the `NSWindow` — no `navigationTitle`/`navigationDocument`/`representedURL` exists anywhere — so every window shows SwiftUI's default title instead of the open folder name, leaving multi-window setups (and Merge All Windows) hard to tell apart. | Medium | SR-2 (spec behaviour unmet) | 2 Foundation/2.2 Global State Management/WindowState.swift:42; App-Sputnik/ContentView.swift | Resolved — 2026-06-11: `ContentView.swift` line 71 applies `.navigationTitle(windowState.title)`; `WindowState.title` line 42 computes folder name. Confirmed in commit 7b13b5b. |
| ISS-029 | 2026-06-10 | 6 Project File Tree / 2.1 Inter-Panel | Vibe Violation | `FileTreeViewModel.openNode` opens files by calling `windowState.openDocument(url:)` directly, bypassing `InterPanelRouter.open(_:)`; `DocumentSession`'s own doc comment states modules must never create sessions directly but route through the 2.1 router, so the file tree breaks the one-way cross-module contract. | Low | SR-1 (cross-module comms route through Foundation 2.1 router) | 6 Project File Tree/FileTreeViewModel.swift:115 | Resolved — 2026-06-10: `FileTreeViewModel` now holds a weak `router: (any InterPanelRouter)?`; `openNode` calls `router?.open(id)`; `selectRootDirectory` calls `router?.syncDirectory(url)`. `FileTreePanel` receives the router via `init(router:)` and passes it to `viewModel.configure(windowState:router:)`. |
| ISS-030 | 2026-06-10 | 3.1 Text | Bug | Opening a document never loads its contents into the editor: `ContentView.onChange(activeDocumentID)` calls `EditorViewModel.resetForNewFile(url:)`, which only resets flags and never reads the file or sets the `NSTextView` string, so every opened file shows a blank editor. | High | SR-2 (core function must work) | App-Sputnik/ContentView.swift:51; 3 Text Editor/3.1 Text/EditorViewModel.swift:52 | Resolved — 2026-06-11: `openDocument(_:)` reads file content via `String(contentsOf:encoding:)`, sets `loadedText`/`loadToken`, `updateNSView` applies to `NSTextView`. |
| ISS-031 | 2026-06-10 | 3.1 Text | Missing Feature | `EncodingGuard` (file-size and encoding/binary protection, spec 3.1.8) is implemented but never called — no code path runs it before loading a file, so there is no guard against opening oversized or binary files and exhausting RAM. | High | SR-3 (refuse oversized/binary files); SR-2 | 3 Text Editor/3.1 Text/EncodingGuard.swift | Resolved — 2026-06-11: `EncodingGuard.validate(url)` called in `openDocument(_:)` line 116, encoding used for `String(contentsOf:encoding:)`. |
| ISS-032 | 2026-06-10 | 3.1 Text | Missing Feature | `SyntaxHighlighter` (spec 3.1.2) is implemented but never invoked — `EditorView.Coordinator.textDidChange` never calls it and no initial highlight pass runs on load, so syntax highlighting never appears in any mode. | Medium | SR-2 (spec requirement unmet) | 3 Text Editor/3.1 Text/SyntaxHighlighter.swift; 3 Text Editor/3.1 Text/EditorView.swift:165 | Resolved — 2026-06-11: Initial highlight in `updateNSView` (lines 128–131); debounced re-highlight in `textDidChange` (lines 200–204). |
| ISS-033 | 2026-06-10 | 3.1 Text | Missing Feature | `FileWatcher` (external file-watching via `NSFilePresenter`, spec 3.1.4) is implemented but never instantiated for the open document, so external edits/moves are not detected and the reload-or-keep prompt never fires. | Medium | MR-2 (FilePresenter for external changes); SR-2 | 3 Text Editor/3.1 Text/FileWatcher.swift | Resolved — 2026-06-11: `startWatchingFile(url:)` creates `FileWatcher` in `openDocument` line 138; stopped on close/deinit. |
| ISS-034 | 2026-06-10 | 3.1 Text | Missing Feature | The find/replace UI (`SearchBarView`, spec 3.1.5) is never mounted in the view hierarchy: ⌘F toggles `SearchController.isVisible` but nothing observes it or renders the bar, and `search()` is never triggered, so Find/Replace is unreachable despite the controller logic being complete. | Medium | SR-2 (spec requirement unmet) | 3 Text Editor/3.1 Text/SearchBarView.swift; App-Sputnik/ContentView.swift:46 | Resolved — 2026-06-11: `SearchBarView` mounted in `TextEditorPanel` (gated on `searchController`); `EditorTextView.keyDown` toggles visible; debounced search on term change. |
| ISS-035 | 2026-06-10 | 3.1 Text | Missing Feature | Save / Save As (spec 3.1.6) is not implemented anywhere in module 3 — there is no code to write the editor buffer back to disk, so edits cannot be persisted. | Medium | SR-2 (spec requirement unmet; data loss) | 3 Text Editor/3.1 Text/ | Resolved — 2026-06-11: `save()`/`saveAs(to:)` with atomic write; menu items (⌘S, ⇧⌘S) wired via `EditorCommandHandling` protocol. |
| ISS-036 | 2026-06-10 | 3.1 Text | Missing Feature | `CrashRecoveryStore` (auto-save + crash recovery via periodic temp-cache serialisation, spec 3.1.7) is implemented but never instantiated, so editor state is never serialised and no recovery cache is written. | Medium | SR-2 (spec requirement unmet) | 3 Text Editor/3.1 Text/CrashRecoveryStore.swift | Resolved — 2026-06-11: Lazy `recoveryStore` in `EditorViewModel`; debounced write triggered from `Coordinator.textDidChange`. |
| ISS-037 | 2026-06-10 | 3.1 Text | Missing Feature | There is no mode-picker UI (the guide's `[Mode: Plain Text ▾]` control), and `EditorViewModel.mode` is only ever set to `.plainText` (by `resetForNewFile`); nothing infers mode from the file extension, so the editor is permanently stuck in plain-text mode and the 3.2/3.3/3.4 providers (dispatched by `mode`) never activate. | Medium | SR-2 (spec requirement unmet) | 3 Text Editor/3.1 Text/EditorViewModel.swift:56; App-Sputnik/ContentView.swift | Resolved — 2026-06-11: Segmented mode picker in `TextEditorPanel`; `modeForFileType(_:)` infers mode from file extension; called in `openDocument`. |
| ISS-038 | 2026-06-10 | 3.4 Html language | Missing Feature | `HTMLDocTypeGuard` is never called on file open, so `EditorViewModel.htmlModeActive` is always `false`; HTML ghost-text suggestions and the "Render as HTML" menu item (both gated on that flag) are therefore permanently dormant. | Medium | SR-2 (spec requirement unmet) | 3 Text Editor/3.4 HTML Langugage/HTMLDocTypeGuard.swift; 3 Text Editor/3.1 Text/EditorViewModel.swift:36 | Resolved — 2026-06-11: `HTMLDocTypeGuard.check(text, viewModel:)` called in `openDocument` line 130. |
| ISS-039 | 2026-06-10 | 3.5 Spelling and Grammar Check | Missing Feature | `SpellCheckFileTypeGuard` is never called on file open, so `EditorViewModel.spellCheckActive` is always `false`; `SpellingGrammarChecker` self-gates on that flag (`guard viewModel?.spellCheckActive == true`), so real-time spelling/grammar checking and quickfix never run even for `.txt`/`.md`. | Medium | SR-2 (spec requirement unmet) | 3 Text Editor/3.5 Spelling and Grammar Checking/SpellCheckFileTypeGuard.swift; 3 Text Editor/3.5 Spelling and Grammar Checking/SpellingGrammarChecker.swift:55 | Resolved — 2026-06-11: `SpellCheckFileTypeGuard.check(url, viewModel:)` called in `openDocument` line 131. |
| ISS-040 | 2026-06-10 | 3.4 Html language | Missing Feature | `RenderAsHTMLCommand` (the File-menu "Render as HTML" action, ⌘⌥P, spec 3.4) is implemented but never inserted into the menu, so the user cannot open the HTML Preview panel from the editor. | Medium | SR-2 (spec requirement unmet) | 3 Text Editor/3.4 HTML Langugage/RenderAsHTMLCommand.swift | Resolved — 2026-06-11: "Render as HTML" (⌘⌥P) in File menu via `EditorCommandHandling.renderAsHTML()`; delegates to `EditorViewModel`. |
| ISS-041 | 2026-06-10 | 3.3 ASCII art | Missing Feature | The Tier-2 ASCII Studio (`ASCIIStudioPanel`, opened by ⌘⌥A or Format → ASCII Studio, spec 3.3) has no trigger wired — no menu item or shortcut presents the panel — so image-to-ASCII conversion and the clipart library are unreachable. | Medium | SR-2 (spec requirement unmet) | 3 Text Editor/3.3 ASCII art/ASCIIStudioPanel.swift | Resolved — 2026-06-11: "ASCII Studio" (⌘⌥A) in Format menu via `EditorCommandHandling.showASCIIStudio()`; delegates to `EditorViewModel`. |
| ISS-042 | 2026-06-10 | 4 Markdown Preview | Design Gap | `MarkdownPreviewViewModel` parses with `interpretedSyntax: .inlineOnlyPreservingWhitespace`, which renders inline markup only (bold, italic, inline code, links). Headings, tables, lists, blockquotes, horizontal rules and fenced code blocks are shown as literal source text (`# Heading`, `\| col \|`, `> quote`), contradicting the guide's mockup and Technical Summary step 2 ("headings … tables, lists, and block quotes rendered"). Switching to `.full` is insufficient on its own: `AttributedString(markdown:)` emits `PresentationIntent` that `NSTextView` does not visually style — a custom intent→attribute styling pass is required to match the guide. | Medium | SR-2 (guide/spec behaviour unmet) | 4 Markdown Preview/MarkdownPreviewViewModel.swift:88; 1 Setup/Module Guides/4 Markdown Preview/guide.md | Resolved — 2026-06-11: `.full` parsing + full `applyPresentationIntentStyling` pipeline implemented; headings, code blocks, blockquotes, and lists render visually. Tables left as plain inline text (grid layout deferred — see comments in `applyKindAttributes`).
| ISS-043 | 2026-06-10 | 8 HTML Preview | Bug | The "More Context" selection capture relies on an injected `WKUserScript` (`selectionchange` → `selectionChange` message handler), but `configuration.defaultWebpagePreferences.allowsContentJavaScript = false` ([HTMLPreviewView.swift:131]) also suppresses app-injected user scripts in `WKWebView`, so `capturedSelection` likely never populates and the HTML-preview right-click help menu never appears. ISS-010 marked this resolved, but the resolution may not function at runtime — needs verification once the project builds. | Medium | SR-2 (feature unmet); SR-5 (use framework correctly) | 8 HTML Preview/HTMLPreviewView.swift:131; 8 HTML Preview/HTMLPreviewCoordinator.swift | Resolved — 2026-06-11: Verified against Apple documentation — `WKUserScript` objects injected via `userContentController.addUserScript()` are NOT blocked by `allowsContentJavaScript = false` (which only prevents JavaScript from page content, not from WKWebView configuration-level scripts). Code has been in place since ISS-010 resolution. Runtime verification deferred until Terminal module build errors are resolved.
| ISS-044 | 2026-06-10 | 4 Markdown Preview | Bug | `MarkdownPreviewPanel` renders `.ascii` files through the Markdown pipeline, but its wrong-type/placeholder copy still reads "No Markdown file open" / "Open a Markdown file to preview", so the panel's empty- and wrong-type states do not acknowledge ASCII as a supported type. | Low | — (copy vs behaviour inconsistency) | 4 Markdown Preview/MarkdownPreviewPanel.swift:233 | Resolved — 2026-06-11: Empty state shows "No file open"; ASCII placeholder shows "ASCII file selected — open a Markdown file to preview"; non-Markdown shows "Plain text file selected — open a Markdown file to preview".
| ISS-045 | 2026-06-10 | 9 Resources | Bug | Module 9's `Package.swift` had no `resources:` block, so all JSON indexes, `.md` topic bodies, ASCII `.txt` art files, and completion corpora were never bundled. Every loader used `Bundle.main.url(forResource:)` which cannot find SPM library resources. Result: all four help panels loaded zero topics, completion corpus was empty, and ASCII library had no records. | Critical | SR-2 (crash proof — silent emptiness) | 9 Resources/Package.swift; 9 Resources/9.1 ASCII Library/ASCIILibrary.swift; 9 Resources/9.2 ASCII art Help/ASCIIArtHelpIndex.swift; 9 Resources/9.2 ASCII art Help/ASCIIArtHelpPanelView.swift; 9 Resources/9.3 Markdown Help/MarkdownHelpIndex.swift; 9 Resources/9.4 Html Help/HTMLHelpIndex.swift; 9 Resources/9.5 Grammar Help/GrammarHelpIndex.swift; 9 Resources/SputnikCompletionCorpus.swift; 3 Text Editor/3.3 ASCII art/ASCIILibraryBrowser.swift | Resolved — 2026-06-10: Added `resources:` block to `9 Resources/Package.swift` with `.process()` for all five resource directories; switched all loaders from `Bundle.main` to `Bundle.module` (module 9) or `Bundle.resourcesModule` (module 3); added `Bundle.resourcesModule` public accessor; corrected subdirectory paths to match actual directory names; added `ResourcesModule` dependency to `TextEditorModule/Package.swift`.
| ISS-046 | 2026-06-10 | 4 Markdown Preview | Missing Feature | Inline image references (`![alt](photo.png)`) never display. `MarkdownPreviewViewModel` parses via `AttributedString(markdown:)`, whose parser drops image markup, and the result renders in an `NSTextView` (no image support). The render path also only receives the raw `String`, not the document's base directory, so relative image paths could not be resolved even if parsed. | Medium | SR-3 (low RAM — must bound decoded image size); SR-5 (native frameworks) | 4 Markdown Preview/MarkdownPreviewViewModel.swift:74; 4 Markdown Preview/MarkdownRenderView.swift | Resolved — 2026-06-10: `PreviewImageResolver` added (module 9); `MarkdownPreviewViewModel.render(markdown:baseDir:)` threads the base directory and post-processes parsed `AttributedString` to insert `NSTextAttachment`s for local image references; remote images render as labelled links. |
| ISS-047 | 2026-06-10 | 8 HTML Preview | Bug | Local `<img src="photo.png">` references render as broken images. `HTMLPreviewView` loads via `loadHTMLString(_:baseURL:)` ([HTMLPreviewView.swift:177]); on macOS a `file://` `baseURL` does not grant WKWebView sandbox read access to sibling files, so relative local images fail to load. Remote `http(s)` images are unaffected. | Medium | SR-3 (low RAM); SR-5 (use WebKit correctly) | 8 HTML Preview/HTMLPreviewView.swift:167 | Resolved — 2026-06-10: `SputnikImageSchemeHandler` registered for `sputnik-img://` scheme; local `<img src="…">` paths rewritten to `src="sputnik-img://host/…"` in preprocessing; handler calls `PreviewImageResolver.data(…)` to stream downsampled bytes. Remote `http(s)` images unaffected. |
| ISS-048 | 2026-06-10 | 5 PDF Viewer / 2.1 Inter-Panel | Missing Feature | PNG/JPEG files cannot be opened for viewing anywhere. `FileType(url:)` classifies `png/jpg/jpeg` as `.binary` ([FileType.swift:24]), which has no viewer route. The PDF Viewer could display them with zero new view code via `PDFPage(image:)` (single-page `PDFDocument`), reusing its existing fit/zoom/rotate/print controls and size-limit pattern, but there is no `.image` FileType case and no routing to send images to the PDF panel. | Low | SR-3 (low RAM — bound decoded size); SR-5 (PDFKit) | 2 Foundation/2.1 Inter-Panel communication/FileType.swift:24; 5 PDF Viewer/PDFViewerViewModel.swift | Resolved — 2026-06-10: `FileType.image` added; `png/jpg/jpeg` reclassified; `AppInterPanelRouter.open(_:)` routes `.image` to PDF Viewer panel; `PDFViewerViewModel.loadImage(_:)` wraps downsampled `NSImage` via `PDFPage(image:)`. |
| ISS-049 | 2026-06-11 | 4 Markdown Preview | Bug | `MarkdownPreviewPanel.handleActiveDocumentChange` assigns `AttributedString()` (Swift type) to `viewModel.renderedString` which is typed `NSAttributedString` — two assignment sites (lines 289 and 299). This is a type mismatch and a compile error. | High | SR-2 (must compile) | 4 Markdown Preview/MarkdownPreviewPanel.swift:289,299 | Resolved — 2026-06-11: Both assignment sites use `NSAttributedString()`. Foundation module builds cleanly.
| ISS-050 | 2026-06-11 | 2.3 Settings / 3.5 Spelling and Grammar | Design Problem | `SettingsStore.spellCheckDebounceInterval` drives two unrelated behaviours — the squiggly-underline instant-correction check (`SpellingGrammarChecker`) and the ghost-text auto-complete suggestions (`SpellingCompletionProvider`) — so there is no way to control auto-complete response speed independently from squiggly-underline latency, violating SR-1 (each setting has one clear meaning). | Medium | SR-1 (each setting owned once, with one clear meaning) | 2 Foundation/2.3 Settings/SettingsStore.swift:125; 3 Text Editor/3.5 Spelling and Grammar Checking/SpellingGrammarChecker.swift:58; 3 Text Editor/3.5 Spelling and Grammar Checking/SpellingCompletionProvider.swift:45 | Resolved — 2026-06-11: `SpellingCompletionProvider` now reads `spellingAutoCompleteStep.timeInterval`; `SpellingGrammarChecker` continues to read `spellCheckDebounceInterval` unchanged. |
| ISS-051 | 2026-06-11 | 2.3 Settings | Design Problem | `SettingsStore.loadAll()` is a ~60-line method containing dozens of repetitive `if let saved: … = persistence.loadSetting(forKey: …)` blocks. It mixes the settings model with deserialisation orchestration, violating SR-6 (one responsibility per file). The loading logic belongs in or alongside `PersistenceService` (2.5), where it can be tested independently. | Low | SR-6 (one responsibility per file) | 2 Foundation/2.3 Settings/SettingsStore.swift:398-501 | Resolved — 2026-06-12: `loadFromDefaults()` extracted into `SettingsLoader` in `2 Foundation/2.5 Persistence/SettingsLoader.swift`. `SettingsStore.loadFromDefaults()` now delegates to `SettingsLoader(persistence:).load(into:)`. SettingsStore.swift dropped from 502 to 377 lines. |
| ISS-052 | 2026-06-11 | 2.2 Global State Management | Design Problem | `AppState` is declared `@unchecked Sendable` — this suppresses the compiler's Sendable checking without providing actual data-race guarantees. While all mutation happens on `@MainActor`, the `@unchecked` annotation means any future off-actor access to `@Observable` published properties would be a data race the compiler would not catch, and the annotation is unnecessary if the class truly never crosses isolation boundaries. | Low | SW-1 (strict actor isolation, no data races) | 2 Foundation/2.2 Global State Management/AppState.swift:12 | Open |
| ISS-053 | 2026-06-11 | 4 Markdown Preview | Bug | `parseIntentKind(_:)` (line 60) is a stub that always returns `nil`. Its signature takes `Any` because `run.presentationIntent` is type-erased (`Any?`). The function never extracts the actual `NSPresentationIntent` identity, so `applyPresentationIntentStyling` (line 39) collects zero intents and applies no visual styling — headings, code blocks, blockquotes, and lists render as plain text. This was originally logged in ISS-042 as resolved, but the intent-parsing implementation was never completed. | High | SR-2 (spec requirement unmet — structured Markdown headings/code/lists render as plain text) | 4 Markdown Preview/MarkdownPreviewViewModel.swift:60-62 | Open |
| ISS-054 | 2026-06-11 | 4 Markdown Preview | Design Problem | `MarkdownPreviewViewModel.swift` (366 lines) mixes five concerns in one file: `ParsedIntentKind` enum + heading font constants (25 lines), `PresentationIntent` styling functions (88 lines), `SendableAttributedString` wrapper (5 lines), `MarkdownPreviewViewModel` class (123 lines), and file-scope async render helpers (120 lines). Violates SR-6 (one responsibility per file). | Medium | SR-6 (one responsibility per file) | 4 Markdown Preview/MarkdownPreviewViewModel.swift:1-366 | Open |
| ISS-055 | 2026-06-11 | 4 Markdown Preview | Design Problem | The `PresentationIntent` rendering pipeline uses `run.presentationIntent` (a private `NSPresentationIntent?` ObjC-bridged type) with `parseIntentKind(_ intent: Any)`. The `Any` type erases all compile-time safety. Even when implemented, the logic depends on undocumented `NSPresentationIntent` selectors that could change across macOS releases, silently breaking styled rendering. | Medium | SR-5 (use existing macOS frameworks correctly — private API dependency is fragile) | 4 Markdown Preview/MarkdownPreviewViewModel.swift:43-45, 60-62 | Open |
| ISS-056 | 2026-06-11 | 3.1 Text | Design Problem | `EditorViewModel` looks up its dependencies by walking `NSApp.delegate as? AppDelegate` in three places: `init()` (line 80) to register the command handler, `recoveryStore` lazy init (line 66) to find `PersistenceService`, and `renderAsHTML()` (line 268) to reach the router. This is a global singleton anti-pattern — brittle in tests, breaks if the app delegate type changes, and is inconsistent with the rest of the codebase where dependencies are injected via `init`. | Medium | SR-1 (modular design — dependencies injected, not looked up) | 3 Text Editor/3.1 Text/EditorViewModel.swift:66-73, 80-86, 268-273 | Open |
| ISS-057 | 2026-06-11 | 3.1 Text | Efficiency | `SyntaxHighlighter.highlight(mode:)` re-highlights the entire document on every text change — clears all `.foregroundColor` attributes from the full range, runs regexes on the entire text, and re-applies attributes. For large files this is O(n × m) per keystroke where n = document length and m = number of regex patterns. Most modern editors do incremental/range-based highlighting. | Low | SR-4 (fast and efficient) | 3 Text Editor/3.1 Text/SyntaxHighlighter.swift:28-43 | Open |
| ISS-056 | 2026-06-12 | 8 HTML Preview | Security Bug | `LinkNavigationPolicy.decide(for:targetIsBlank:currentBaseURL:)` checked `targetIsBlank` (line 55) BEFORE the scheme switch (line 58), so a `javascript:` or `data:` URL with `target="_blank"` was returned as `.openExternally` instead of `.block`. This would cause `NSWorkspace.shared.open()` to be called on a `javascript:` or `data:` URL. While NSWorkspace currently silently no-ops for these schemes, the routing was incorrect and a future system change could make it harmful. | High | SR-2 (handle all failure paths; no force-unwraps in production paths) | 8 HTML Preview/LinkNavigationPolicy.swift:55-57 | Resolved — 2026-06-12: Scheme switch moved above the `targetIsBlank` check; `javascript`, `data`, and `blob` cases always return `.block` regardless of `targetIsBlank`. Tests `targetBlankTakesPriorityOverJavascriptScheme` and `targetBlankTakesPriorityOverDataScheme` renamed and updated to assert `.isBlock`. |
| ISS-058 | 2026-06-12 | 6 Project File Tree | Missing Feature | The File Tree lacks five quality-of-life features expected in a modern file browser: (1) drag-to-move within the tree — `onDrop` only exists on the root ScrollView, individual folders cannot accept drops; (2) inline rename — uses an NSAlert text-field prompt instead of clicking the name to edit in-place; (3) multi-select — `selectedNodeID` is a single `URL?` instead of `Set<URL>`; (4) keyboard shortcuts — no ⌘⌫ for trash, ⏎ for rename, ⌘N for new file; (5) file metadata display — `modificationDate` is collected in `FileTreeNode` but never shown. | Low | — | 6 Project File Tree/FileTreeViewModel.swift:20; 6 Project File Tree/FileTreePanel.swift:240-268; 6 Project File Tree/FileContextMenu.swift:54-58; 6 Project File Tree/FileTreeRowView.swift:65-66 | Open |
| ISS-060 | 2026-06-12 | 2 Foundation | Design Problem | Nine classes in Foundation use `@unchecked Sendable` to suppress Sendable checking: `AppState`, `SettingsStore`, `WindowState`, `DebounceTimer`, `RenderThrottle`, `SupportingAIMonitor`, `ErrorReporting` (actor), `PreviewImageCache` (actor), and external files `FileWatcher` (3.1), `FileSystemWatcher` (6). A plan exists for `AppState` only (ISS-052). The remaining eight need audit: `@MainActor` classes can remove it; actors need a different approach. | Medium | SW-1 (strict actor isolation, no data races) | 2 Foundation/2.2 Global State Management/AppState.swift; .../WindowState.swift; 2 Foundation/2.3 Settings/SettingsStore.swift; 2 Foundation/2.7 Utilities/DebounceTimer.swift; .../RenderThrottle.swift; .../SupportingAIMonitor.swift; .../ErrorReporting.swift; .../PreviewImageCache.swift; 3 Text Editor/3.1 Text/FileWatcher.swift; 6 Project File Tree/FileSystemWatcher.swift | Open |
| ISS-060 | 2026-06-12 | 2 Foundation | Design Problem | Nine classes in Foundation use `@unchecked Sendable` (see ISS-060 above). This issue tracks the `DebounceTimer`, `RenderThrottle`, `PreviewImageCache`, and `ErrorReporting` utility types specifically. These four are shared utilities that should be extracted from Foundation into a dedicated shared target (e.g. `SputnikShared`) so Foundation can focus on state and routing. Moving them also forces the `@unchecked Sendable` issue to be resolved at the new location. | Low | SR-6 (one responsibility per module); SR-1 (Foundation is an interface layer, not a utility dump) | 2 Foundation/2.7 Utilities/DebounceTimer.swift; .../RenderThrottle.swift; .../PreviewImageCache.swift; .../ErrorReporting.swift | Open |
| ISS-059 | 2026-06-12 | 7 Terminal | Missing Feature | The terminal has no text selection, copy, or paste support. `TerminalTextView` draws cells with raw Core Text and is not an `NSTextView` — there is no `mouseDragged` selection tracking, no `selectedRange`, no ⌘C to copy terminal output, and no ⌘V to paste clipboard content into the shell. The `keyDown(with:)` handler filters out Command-combos (via `KeyEncoder`), so ⌘C/⌘V are silently swallowed. | Low | — | 7 Terminal/TerminalTextView.swift:132-148 | Open |
| ISS-060 | 2026-06-12 | 4 Markdown Preview | Bug | `parseIntentKind(_ intent: Any) -> ParsedIntentKind?` in `MarkdownPreviewViewModel.swift` (line 61) always returns `nil`. This makes the entire `PresentationIntent` styling pipeline a no-op: `applyPresentationIntentStyling` collects no intents and applies no visual attributes, so headings, code blocks, blockquotes, and list items all render as unstyled plain text despite the infrastructure existing. Related: ISS-055 (API fragility of the `Any`-typed `parseIntentKind` signature). | Medium | SR-2 (spec requirement unmet — styled rendering described in guide) | 4 Markdown Preview/MarkdownPreviewViewModel.swift:60-62 | Open |
| ISS-061 | 2026-06-12 | 4 Markdown Preview | Missing Feature | `MarkdownPreviewViewModel.scrollOffset: CGFloat` is declared, updated in `applyRenderedResult`, and described in the guide as "preserved vertical scroll position across re-renders". However, no code in `MarkdownRenderView` or `MarkdownPreviewPanel` reads or writes `scrollOffset` — there is no `ScrollViewReader`, no `.scrollPosition` binding, and no `NSTextView` scroll offset restoration wired to the property. Scroll position is not preserved across document switches. | Low | SR-2 (documented spec behaviour unmet) | 4 Markdown Preview/MarkdownPreviewViewModel.swift:145; 4 Markdown Preview/MarkdownPreviewPanel.swift | Open |
